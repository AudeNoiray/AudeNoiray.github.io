<ul class="card-text font-weight-light list-group list-group-flush">
  {% assign scientificActivities = data[1] | sort: 'date' | reverse %}
  {% for activity in scientificActivities %}
    <li class="list-group-item">
      <div class="row">
        <div class="col-xs-2 cl-sm-2 col-md-2 text-center date-column">
          {% if activity.date %}
            <table class="table-cv">
              <tbody>
                <tr>
                  <td>
                    <span class="badge font-weight-bold danger-color-dark text-uppercase align-middle" style="min-width: 75px">
                      {{ activity.date }}
                    </span>
                  </td>
                </tr>
              </tbody>
            </table>
          {% endif %}
        </div>
        <div class="col-xs-10 cl-sm-10 col-md-10 mt-2 mt-md-0">
          <h6 class="ml-1 ml-md-4 mb-1" style="font-size: 0.95rem;">
            <strong>{{ activity.title }}</strong>
            {%- if activity.description %}, {{ activity.description | strip_newlines | strip }}{% endif %}
          </h6>
        </div>
      </div>
    </li>
  {% endfor %}
</ul>
